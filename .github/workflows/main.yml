name: RDP Automático

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'  # Ejecutar cada 6 horas
  push:
    branches:
      - main

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    concurrency:
      group: rdp-session
      cancel-in-progress: false

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Enable Remote Desktop and disable Network Level Authentication (if needed)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          # Remove any existing rule with the same name to avoid duplication
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          
          # For testing, allow any incoming connection on port 3389
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389

          # (Optional) Restart the Remote Desktop service to ensure changes take effect
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          # Verificar si el usuario ya existe
          $userExists = Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue
          
          if (-not $userExists) {
              Add-Type -AssemblyName System.Security
              $charSet = @{
                  Upper   = [char[]](65..90)      # A-Z
                  Lower   = [char[]](97..122)     # a-z
                  Number  = [char[]](48..57)      # 0-9
                  Special = ([char[]](33..47) + [char[]](58..64) +
                             [char[]](91..96) + [char[]](123..126)) # Special characters
              }
              $rawPassword = @()
              $rawPassword += $charSet.Upper | Get-Random -Count 4
              $rawPassword += $charSet.Lower | Get-Random -Count 4
              $rawPassword += $charSet.Number | Get-Random -Count 4
              $rawPassword += $charSet.Special | Get-Random -Count 4
              $password = -join ($rawPassword | Sort-Object { Get-Random })
              $securePass = ConvertTo-SecureString $password -AsPlainText -Force
              New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member "RDP"
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          } else {
              $password = "ExistingUser"
          }
          
          "RDP_USER=RDP" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "RDP_PASSWORD=$password" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install Tailscale
        run: |
          # Verificar si Tailscale ya está instalado
          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"
          if (-not (Test-Path $tsPath)) {
              Write-Host "Instalando Tailscale..."
              $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
              $installerPath = "$env:TEMP\tailscale.msi"
              
              try {
                  Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath -ErrorAction Stop
          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"
          
          # Verificar si TAILSCALE_AUTH_KEY existe
          if ([string]::IsNullOrEmpty("${{ secrets.TAILSCALE_AUTH_KEY }}")) {
              Write-Error "⚠️  TAILSCALE_AUTH_KEY no configurado. Por favor:"
              Write-Error "1. Ve a: https://login.tailscale.com/admin/settings/keys"
              Write-Error "2. Crea una Auth Key (reutilizable)"
              Write-Error "3. En GitHub Repo > Settings > Secrets > New Repository Secret"
              Write-Error "4. Nombre: TAILSCALE_AUTH_KEY"
              exit 1
          }
          
          Write-Host "Conectando a Tailscale..."
          & $tsPath up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname="maqukan-$env:GITHUB_RUN_ID" --accept-routes --accept-dns=false
          
          # Esperar a que Tailscale asigne IP
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 15) {
              $tsIP = & $tsPath ip -4 2>$null
              if ($tsIP) { break }
              Start-Sleep -Seconds 4
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "❌ No se asignó IP de Tailscale"
              exit 1
          }
          
          Write-Host "✅ IP de Tailscale asignada: $tsIP"
          "TAILSCALE_IP=$tsIP" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
          Write-Host "`n╔════════════════════════════════════════════════╗"
          Write-Host "║       🔐 RDP ACCESO CONFIGURADO ✅             ║"
          Write-Host "╠════════════════════════════════════════════════╣"
          Write-Host "║ Dirección: $env:TAILSCALE_IP"
          Write-Host "║ Usuario:   $env:RDP_USER"
          Write-Host "║ Contraseña: $env:RDP_PASSWORD"
          Write-Host "║ Puerto:    3389"
          Write-Host "║ Protocolo: RDP (Remote Desktop)"
          Write-Host "╠════════════════════════════════════════════════╣"
          Write-Host "║ 1. Abre Cliente RDP en tu máquina"
          Write-Host "║ 2. Ingresa: $env:TAILSCALE_IP:3389"
          Write-Host "║ 3. Usuario y contraseña como arriba"
          Write-Host "║ 4. ¡Listo!"
          Write-Host "╚════════════════════════════════════════════════╝`n"
          
          # Mantener activo por 6 horas
          $endTime = (Get-Date).AddHours(6)
          while ((Get-Date) -lt $endTime) {
              Write-Host "[$(Get-Date -Format 'HH:mm:ss')] RDP Activo - Sesión disponible"
              Start-Sleep -Seconds 300
          }
          
          Write-Host "`n✅ Sesión completada correctamente"uterName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }Display RDP Access Information
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: $env:RDP_USER"
          Write-Host "Password: $env:RDP_PASSWORD"
          Write-Host "Port: 3389"
          Write-Host "==================`n"
          
          # Keep runner active (GitHub provides 35+ minutes per job)
          Write-Host "RDP is now accessible via Tailscale"
          Start-Sleep -Seconds 120hile ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
              Start-Sleep -Seconds 300
          }
